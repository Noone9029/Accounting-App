# Frontend Redesign Route Family Checklist

Date: 2026-06-22

Branch: `codex/ui-rebuild-loop-full-frontend`

This checklist tracks the full LedgerByte frontend redesign loop. It is a product/UI tracking artifact only. It does not add backend behavior, provider integrations, storage behavior, signed URLs, compliance submission, hosted mutations, seed/reset/delete commands, or deployment behavior.

| Route family | Routes inspected | Migrated | Placeholder/deferred | Tests run | Visual check | Mobile check | Accessibility notes | Permission notes | Remaining gaps |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Sales workspace | `/sales/invoices`, `/sales/invoices/new`, `/sales/invoices/[id]`, `/sales/invoices/[id]/edit`, `/sales/quotes`, `/sales/quotes/new`, `/sales/quotes/[id]`, `/sales/quotes/[id]/edit`, `/sales/credit-notes`, `/sales/customer-payments`, `/sales/customer-refunds`, `/sales/recurring-invoices`, `/sales/delivery-notes`, `/sales/inventory-returns`, `/sales/collections` | In progress. `/sales/invoices` and `/sales/quotes` list surfaces now use shared `LedgerPage`, `LedgerPageBody`, `LedgerSummaryBand`, `LedgerToolbar`, `LedgerDataTable`, `LedgerEmptyState`, `LedgerMoney`, `LedgerDate`, and `LedgerStatusBadge` patterns. | None marked blocked yet. | `corepack pnpm --filter @ledgerbyte/web test -- route-load-verification sales/quotes sales-quote-form ledger-system`: PASS, 27 tests. `corepack pnpm --filter @ledgerbyte/web typecheck`: PASS. | `/sales/invoices` desktop/mobile PASS via authenticated route hardening. Quote workflow visual reached final route assertions but failed on generic browser 404 console errors; see evidence doc. | `/sales/invoices` mobile PASS via authenticated route hardening and accountant compact route QA. Quote mobile-specific list visual still pending because quote workflow blocker is harness/asset related. | List surfaces preserve headings, semantic tables, status text, filter labels, non-posting quote wording, and explicit invoice-finalize wording. | Sales invoice create/finalize and quote create/update visibility still use existing `PERMISSIONS.salesInvoices.*` gates. | Sales detail pages, forms, credit notes, collections, delivery notes, refunds, recurring invoices, and inventory-return sales routes still need systematic redesign. |
| Purchase workspace | `/purchases/bills`, `/purchases/bills/new`, `/purchases/bills/[id]`, `/purchases/bills/[id]/edit`, `/purchases/purchase-orders`, `/purchases/supplier-payments`, `/purchases/debit-notes`, `/purchases/debit-notes/[id]`, `/purchases/debit-notes/[id]/edit`, `/purchases/supplier-refunds`, `/purchases/cash-expenses`, `/purchases/matching`, `/purchases/ap-dashboard`, `/purchases/returns` | In progress. `/purchases/bills` and `/purchases/debit-notes` list surfaces now use shared `LedgerPage`, `LedgerPageBody`, `LedgerSummaryBand`, `LedgerDataTable`, `LedgerEmptyState`, `LedgerMoney`, `LedgerDate`, `LedgerStatusBadge`, and `LedgerButton` patterns. | None marked blocked yet. | `corepack pnpm --filter @ledgerbyte/web test -- route-load-verification purchase-bill-form purchase-debit-note-form ledger-system`: PASS, 22 tests. `corepack pnpm --filter @ledgerbyte/web typecheck`: PASS. | `/purchases/bills` and `/purchases/debit-notes` desktop/mobile PASS via authenticated route hardening. | `/purchases/bills` and `/purchases/debit-notes` mobile PASS via authenticated route hardening; tables remain horizontally scrollable through `LedgerDataTable`. | List surfaces preserve semantic headings, table headers, explicit AP posting/adjustment wording, visible row actions, and non-color-only status labels. | Existing `PERMISSIONS.purchaseBills.*` and `PERMISSIONS.purchaseDebitNotes.*` create/finalize visibility remains in place. | Purchase bill/debit-note detail pages, forms, purchase orders, supplier payments/refunds, cash expenses, matching, AP dashboard, and purchase returns still need systematic redesign. |
| Banking and reconciliation | `/bank-accounts`, `/bank-accounts/[id]`, `/bank-accounts/[id]/statement-transactions`, `/bank-accounts/[id]/statement-imports`, `/bank-accounts/[id]/reconciliation`, `/bank-accounts/[id]/reconciliations`, deposits, cheques, card settlements, rules, `/bank-transfers`, `/bank-transfers/[id]`, `/bank-reconciliations/[id]`, `/bank-statement-transactions/[id]` | In progress. `/bank-accounts` and `/bank-transfers` list surfaces now use shared `LedgerPage`, `LedgerPageBody`, `LedgerSummaryBand`, `LedgerDataTable`, `LedgerEmptyState`, `LedgerMoney`, `LedgerDate`, `LedgerStatusBadge`, and `LedgerButton` patterns. | Statement transaction review/import/reconciliation routes deferred for a dedicated manual-review loop because they contain inline match/categorize/import/close actions. | `corepack pnpm --filter @ledgerbyte/web test -- route-load-verification bank-accounts bank-transfers ledger-system`: PASS, 68 tests. `corepack pnpm --filter @ledgerbyte/web typecheck`: PASS. | `/bank-accounts` desktop/mobile PASS via authenticated route hardening. `/bank-transfers` list has no existing visual fixture in this branch; detail fixture exists only for `/bank-transfers/[id]`. | `/bank-accounts` mobile PASS; `/bank-transfers` mobile covered by responsive `LedgerDataTable` code path and route-load/typecheck only. | List surfaces preserve semantic headings, manual banking wording, explicit transfer posting truth, table headers, visible actions, and non-color-only status labels. | Existing `PERMISSIONS.bankAccounts.manage` and `PERMISSIONS.bankTransfers.create` visibility remains in place. | Bank account detail, statement transaction review, statement imports, rules, deposits, cheques, card settlements, reconciliation summary/history/close, and transfer detail still need systematic redesign. |
| Contacts, customers, suppliers | `/contacts`, `/contacts/[id]`, `/customers`, `/customers/[id]`, `/customers/[id]/statement`, `/suppliers`, `/suppliers/[id]`, `/suppliers/[id]/statement` | Deferred. | Deferred until Sales/Purchase surfaces settle. | Not run for this branch. | Not run. | Not run. | Need profile header, balances, document tabs, tax fields, and statement accessibility. | Preserve contact and statement permissions. | Needs shared party page migration and statement route review. |
| Inventory, products, services | `/items`, `/inventory/balances`, warehouses, stock movements, adjustments, transfers, batches, serial numbers, bin locations, FIFO preview, landed cost, purchase receipts, sales stock issues, inventory reports, settings, variances | Deferred. | Deferred for inventory-specific loop. | Not run for this branch. | Not run. | Not run. | Must preserve valuation, FIFO, manual COGS, and no fake costing claims. | Preserve inventory view/manage permissions. | Needs dense stock tables, movement history, valuation warnings, and report redesign. |
| Accounting/admin workspaces | `/accounts`, `/journal-entries`, `/journal-entries/new`, `/tax-rates`, `/fiscal-periods`, `/branches`, placeholder accounting routes | Deferred. | Deferred for accounting/admin loop. | Not run for this branch. | Not run. | Not run. | Must keep posting/lock warnings and no silent posting. | Preserve accounting, tax, and branch permissions. | Needs chart table, journal grid, period status, and placeholder honesty pass. |
| Reports | `/reports`, general ledger, trial balance, profit and loss, balance sheet, VAT summary, VAT return, aged receivables, aged payables, report packs | Partially migrated: reports index and report packs from merged stack. | Drilldowns deferred. | Prior merged checks only; pending for this branch. | Prior report visuals only; pending for this branch. | Prior report visuals only. | Must preserve report table semantics and unofficial filing disclaimers. | Preserve report/export permissions. | Needs report drilldown migration and export-control review. |
| Documents and storage | `/documents`, `/settings/documents`, `/settings/storage`, generated-document archive/download surfaces where present | Partially migrated: documents archive from merged stack. | Document detail route absent unless later implemented; storage remains readiness-only. | Prior merged checks only. | Prior merged visual smoke only. | Prior merged visual smoke only. | Preserve archive table semantics and storage readiness wording. | Preserve generated document and document settings permissions. | Needs settings documents/storage redesign pass and attachment route inspection. |
| Settings and admin | `/settings`, team, roles, role detail, number sequences, email outbox, audit logs, documents, storage, banking accounting, security, ZATCA, compliance | Partially migrated: settings overview and representative roles/settings foundation. | Subroutes deferred for settings loop. | Prior merged checks only. | Prior merged settings visual smoke only. | Prior merged settings visual smoke only. | Must keep grouped admin panels, danger zones, and conservative readiness text. | Preserve admin, audit, role, email, compliance, and storage permissions. | Needs subroute-by-subroute shared primitive migration. |
| Compliance readiness | `/settings/zatca`, `/settings/compliance`, UAE readiness panels, storage/security readiness surfaces | Deferred. | Deferred for compliance-specific loop. | Not run for this branch. | Not run. | Not run. | Must preserve local-only/readiness-only wording and avoid production claims. | Preserve ZATCA/compliance permissions. | Needs careful ZATCA/UAE/Peppol/ASP wording and panel migration. |
| Setup/onboarding | `/setup`, `/organization/setup`, typed onboarding components, setup checklist previews | Deferred. | `/onboarding` and `/get-started` absent unless later routed. | Not run for this branch. | Not run. | Not run. | Preserve checklist semantics, form labels, and setup blockers. | Preserve dashboard/setup access rules. | Needs guided first-workflow redesign after operational loops. |
| Dashboard | `/dashboard` | Partially migrated in merged foundation. | Revisit after deeper route-family loops. | Prior merged checks only. | Prior dashboard snapshots from foundation only. | Prior dashboard snapshots only. | Must not invent KPI data. | Preserve dashboard permission visibility. | Needs final attention queue, cash, AR/AP, banking, storage/compliance snapshot pass. |
| Marketing/public/auth | `/`, `/pricing`, `/product`, `/readiness`, `/resources`, `/workflows`, Arabic equivalents, login, register, invite accept, password reset | Auth form foundation migrated; marketing deferred. | Deferred for public/auth loop. | Not run for this branch. | Not run. | Not run. | Preserve accessible form labels/errors and beta posture. | Auth permissions are route/context driven. | Needs marketing credibility pass and auth error-state sweep. |
| Placeholder/future routes | `/(app)/[...placeholder]` and absent future modules identified during route sweeps | Deferred. | Must remain explicitly planned/future, permission-guarded where inside app shell, and not presented as live. | Not run for this branch. | Not run. | Not run. | Preserve heading/landmark and no fake live functionality. | Preserve app-shell permission handling. | Needs placeholder catalog and styling consistency pass. |
