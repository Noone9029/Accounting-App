# Frontend Completion Issues

Goal ID: `FRONTEND-COMPLETION-01`

Branch: `codex/frontend-completion-01`

Baseline inspected: `d5020961`

This file tracks confirmed frontend product-completion findings only. It does not create beta launch, provisioning, roadmap, deployment, backend, schema, provider, storage, compliance, accounting, report, VAT, inventory valuation, banking logic, or hosted-environment work.

| Route | Issue | Severity | Type | Fix status | Test added | Remaining follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| `/bank-accounts`, `/bank-transfers` sidebar category | Banking sidebar exposed `Bank transactions`, `Reconciliation`, and `Cheques` as separate child links even though each pointed to `/bank-accounts`, making the category look more directly routable than it is. | P1 | navigation / broken-link | Fixed by keeping only distinct Banking child destinations: `Bank accounts` and `Bank transfers`. | `apps/web/src/components/app-shell/sidebar.test.tsx` asserts the Banking child link list. | Add direct top-level routes later only if product scope creates real routable banking workflow indexes. |
| `/settings/compliance` sidebar children | Settings sidebar repeated `/settings/compliance` as both `Compliance settings` and `API / provider setup`, which looked like a separate provider setup workflow while no provider connection is live. | P1 | navigation / placeholder | Fixed by removing the duplicate child destination from Settings; `Compliance settings` remains reachable and conservative. | `apps/web/src/components/app-shell/sidebar.test.tsx` asserts Settings child hrefs are not duplicated. | Add a real provider setup route later only after provider scope is approved and implemented. |
| `/` | Public route metadata and route helpers treated `/` as the English marketing home, but the actual app route redirected `/` to `/login`, so the canonical public home did not render. | P1 | broken-link / route | Fixed by rendering the existing `MarketingHomePage` at `/` with matching marketing metadata. | `tests/visual/public-auth-marketing.visual.spec.ts` now checks `/` across desktop/tablet/mobile. | None for English home; Arabic public routes remain optional visual follow-up. |
| `/`, `/product`, `/pricing`, `/login`, `/register`, `/password-reset`, `/invite/accept` | Public/auth/marketing had Jest coverage and live login evidence, but no first-class desktop/tablet/mobile Playwright visual fixture family after the redesign. | P2 | visual | Fixed by adding `public-auth-marketing.visual.spec.ts` with public/auth route headings, key states, overflow checks, and forbidden-claim checks. | `tests/visual/public-auth-marketing.visual.spec.ts`; invite preview fixture added in `tests/visual/visual-fixtures.ts`. | Expand Arabic public routes in a later fixture pass if they require separate screenshot signoff. |

## FRONTEND-COMPLETION-02

Branch: `codex/frontend-completion-02`

Baseline inspected: `ec9ded0e`

This addendum tracks confirmed workflow-screen fixes after PR #218. It remains frontend-only: no backend API, Prisma schema, migration, provider, storage, hosted deployment, accounting logic, ledger posting, report math, VAT math, inventory valuation, or bank reconciliation behavior was changed.

| Route | Issue | Severity | Type | Fix status | Test added | Remaining follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| `/sales/quotes/[id]/edit?returnTo=...` | Quote edit pages ignored `returnTo` because the form only read query context during new-quote customer prefill, so Cancel and post-save returned to `/sales/quotes` instead of the originating customer/statement/workspace. | P1 | navigation / return-to | Fixed by always reading safe `returnTo` from the query string while keeping customer prefill limited to new quote routes. | `apps/web/src/components/forms/sales-quote-form.test.tsx` asserts Cancel and post-save redirect preserve the edit route return target. | None. |
| `/sales/quotes/new` | When no customers were available, the customer select showed only a disabled option and no recovery path, leaving first-quote creation unclear. | P1 | empty-state / workflow | Fixed with an actionable empty state linking to `/customers` before the quote form controls. | `apps/web/src/components/forms/sales-quote-form.test.tsx` asserts the customer-empty guidance and link. | None. |
| `/purchases/bills/new`, `/purchases/bills/[id]/edit` | Bill line table controls lacked row-specific accessible names, making repeated item, description, account, quantity, unit price, discount, and tax controls ambiguous. The last remaining line could also be removed. | P1 | a11y / form | Fixed row-specific `aria-label`s and disabled the remove action when only one line remains. | `apps/web/src/components/forms/purchase-bill-form.test.tsx` asserts line labels and the disabled final remove button. | Consider richer line validation messages later if business rules expand. |
| `/purchases/orders/new`, `/purchases/orders/[id]/edit` | Purchase order line controls had the same repeated-control accessibility gap as bill lines, and the final line remove action stayed active. | P1 | a11y / form | Fixed row-specific `aria-label`s and disabled the remove action when only one line remains. | `apps/web/src/components/forms/purchase-order-form.test.tsx` asserts line labels, disabled final remove, and edit-route return behavior. | None. |
| `/bank-accounts/[id]/statement-transactions` | Dense statement review rows used a generic `Detail` link, which was ambiguous across repeated banking table actions. | P2 | a11y / action hierarchy | Fixed the row drilldown label to `View row detail` without changing the destination or review behavior. | `apps/web/src/app/(app)/bank-accounts/[id]/statement-transactions/page.test.tsx` asserts the specific link text and href. | None. |
| `/bank-reconciliations/[id]` | Captured close rows used a generic `Row` action in the reconciliation audit table, which was ambiguous for keyboard review and table scanning. | P2 | a11y / action hierarchy | Fixed the drilldown label to `Open statement row` without changing reconciliation state or close behavior. | `apps/web/src/app/(app)/bank-reconciliations/[id]/page.test.tsx` asserts the specific link text and href. | None. |
| `/reports/*` report detail pages | Shared report detail headers used a generic `Reports` action instead of explicit back navigation. | P2 | navigation / action hierarchy | Fixed the shared report header action to `Back to reports`. | `apps/web/src/components/reports/report-pages.test.tsx` asserts the explicit report-detail back link. | None. |

## FRONTEND-COMPLETION-03

Branch: `codex/frontend-completion-03`

Baseline inspected: `de911549`

This addendum tracks remaining workflow-depth fixes after PR #219. It remains frontend-only: no backend API, Prisma schema, migration, provider, storage, hosted deployment, accounting logic, ledger posting, report math, VAT math, inventory valuation, or bank reconciliation behavior was changed.

| Route | Issue | Severity | Type | Fix status | Test added | Remaining follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| `/items` | Item catalog rows repeated generic `Edit`, `Traceability`, `Disable`, and `Delete` actions, making dense table actions ambiguous for keyboard and assistive-technology users. | P2 | a11y / action hierarchy | Fixed by keeping compact visible labels while adding item-specific accessible names. | `apps/web/src/app/(app)/items/page.test.tsx` asserts item-specific action names and traceability href. | None. |
| `/inventory/warehouses` | Warehouse rows repeated generic `View`, `Archive`, and `Reactivate` actions across active and archived locations. | P2 | a11y / action hierarchy | Fixed by adding warehouse code/name context to row action accessible names. | `apps/web/src/app/(app)/inventory/inventory-list-actions.test.tsx` asserts warehouse-specific action names. | None. |
| `/inventory/adjustments`, `/inventory/purchase-receipts`, `/inventory/sales-stock-issues`, `/inventory/transfers` | Inventory document lists used repeated generic `View` row actions, which made document drilldowns hard to distinguish in table navigation. | P2 | a11y / action hierarchy | Fixed by adding adjustment, receipt, issue, and transfer numbers to row action accessible names. | `apps/web/src/app/(app)/inventory/inventory-list-actions.test.tsx` asserts document-specific drilldown names and hrefs. | None. |
| `/inventory/balances` | Balance rows repeated `FIFO preview` links, and the guidance panel used one-off link button styling instead of shared Ledger buttons. | P2 | a11y / design-system consistency | Fixed row-specific FIFO accessible names and normalized guidance actions to `LedgerButton`. | `apps/web/src/app/(app)/inventory/inventory-list-actions.test.tsx`; `apps/web/src/app/(app)/inventory/inventory-guidance.test.tsx`. | None. |
| `/inventory/stock-movements`, `/inventory/stock-movements/new` | Stock movement rows repeated generic FIFO/traceability actions, and the new movement page used a vague `Back` header action. | P2 | a11y / navigation | Fixed row-specific FIFO and traceability accessible names and changed the header action to `Back to stock movements`. | `apps/web/src/app/(app)/inventory/inventory-list-actions.test.tsx` asserts movement row labels and back navigation. | None. |
| `/inventory/reports/stock-valuation` | Stock valuation rows repeated generic FIFO and traceability links. | P2 | a11y / report drilldown | Fixed by adding row-specific accessible names for item/warehouse FIFO and item traceability links. | Covered by typecheck and changed-family visual route coverage; add a dedicated Jest fixture later if this report grows more interactions. | Optional future test depth for stock valuation row interactions. |
| `/reports/aged-receivables`, `/reports/aged-payables` | Aging report rows repeated generic `Open invoice` / `Open bill` links even when multiple rows were present. | P2 | a11y / report drilldown | Fixed by adding document numbers to the drilldown accessible names while preserving visible labels and `returnTo`. | `apps/web/src/components/reports/report-pages.test.tsx` asserts row-specific invoice/bill drilldown names and preserved hrefs. | None. |
| `/customers/[id]`, `/suppliers/[id]`, `/customers/[id]/statement`, `/suppliers/[id]/statement`, `/settings/*` | Inspected for obvious ambiguous workflow actions or unsafe frontend claims in this pass. Existing route-backed handoffs, statement return context, and conservative settings wording did not require code changes. | P3 | inspection / no-fix | Documented as inspected; no frontend defect confirmed within this goal's scope. | Existing party/report/settings tests remain the coverage source. | Continue only if future QA identifies a concrete broken action, overflow, or unsafe claim. |
