# Database Model Catalog

Audit date: 2026-05-15

Schema source: `apps/api/prisma/schema.prisma`

This checkpoint adds email/token models for mock invitation and password reset delivery. Tokens store hashes only; mock email bodies are inspectable through tenant-scoped outbox APIs.

## Enums

| Enum | Values | Purpose |
| --- | --- | --- |
| `MembershipStatus` | `ACTIVE`, `INVITED`, `SUSPENDED` | Organization membership state. |
| `EmailDeliveryStatus` | `QUEUED`, `SENT_MOCK`, `FAILED` | Mock/local email delivery status. |
| `EmailTemplateType` | `ORGANIZATION_INVITE`, `PASSWORD_RESET` | Email template classification for outbox records. |
| `AuthTokenPurpose` | `ORGANIZATION_INVITE`, `PASSWORD_RESET` | Purpose boundary for hashed email tokens. |
| `AccountType` | `ASSET`, `LIABILITY`, `EQUITY`, `REVENUE`, `EXPENSE`, `COST_OF_SALES` | Chart of accounts classification. |
| `ContactType` | `CUSTOMER`, `SUPPLIER`, `BOTH` | AR/AP contact role. |
| `TaxRateScope` | `SALES`, `PURCHASES`, `BOTH` | Tax rate use area. |
| `TaxRateCategory` | `STANDARD`, `ZERO_RATED`, `EXEMPT`, `OUT_OF_SCOPE`, `REVERSE_CHARGE` | VAT category placeholder. |
| `JournalEntryStatus` | `DRAFT`, `POSTED`, `VOIDED`, `REVERSED` | Journal lifecycle. |
| `ItemType` | `SERVICE`, `PRODUCT` | Item classification. |
| `ItemStatus` | `ACTIVE`, `DISABLED` | Item availability. |
| `SalesInvoiceStatus` | `DRAFT`, `FINALIZED`, `VOIDED` | Invoice lifecycle. |
| `CreditNoteStatus` | `DRAFT`, `FINALIZED`, `VOIDED` | Credit note lifecycle. |
| `PurchaseOrderStatus` | `DRAFT`, `APPROVED`, `SENT`, `PARTIALLY_BILLED`, `BILLED`, `CLOSED`, `VOIDED` | Purchase order lifecycle. |
| `PurchaseBillStatus` | `DRAFT`, `FINALIZED`, `VOIDED` | Purchase bill lifecycle. |
| `CustomerPaymentStatus` | `DRAFT`, `POSTED`, `VOIDED` | Customer payment lifecycle. |
| `SupplierPaymentStatus` | `DRAFT`, `POSTED`, `VOIDED` | Supplier payment lifecycle. |
| `CustomerRefundStatus` | `DRAFT`, `POSTED`, `VOIDED` | Customer refund lifecycle. |
| `CustomerRefundSourceType` | `CUSTOMER_PAYMENT`, `CREDIT_NOTE` | Refund source type. |
| `BankAccountType` | `BANK`, `CASH`, `WALLET`, `CARD`, `OTHER` | Cash/bank profile classification for posting asset accounts. |
| `BankAccountStatus` | `ACTIVE`, `ARCHIVED` | Cash/bank profile availability. |
| `BankTransferStatus` | `POSTED`, `VOIDED` | Bank transfer lifecycle. |
| `BankStatementImportStatus` | `IMPORTED`, `PARTIALLY_RECONCILED`, `RECONCILED`, `VOIDED` | Statement batch reconciliation state. |
| `BankStatementTransactionStatus` | `UNMATCHED`, `MATCHED`, `CATEGORIZED`, `IGNORED`, `VOIDED` | Imported statement row state. |
| `BankStatementTransactionType` | `DEBIT`, `CREDIT` | Bank statement movement direction; credit increases bank balance and debit decreases it. |
| `BankStatementMatchType` | `JOURNAL_LINE`, `MANUAL_JOURNAL`, `CASH_EXPENSE`, `CUSTOMER_PAYMENT`, `SUPPLIER_PAYMENT`, `CUSTOMER_REFUND`, `SUPPLIER_REFUND`, `BANK_TRANSFER`, `OTHER` | Manual reconciliation match classification. |
| `BankReconciliationStatus` | `DRAFT`, `PENDING_APPROVAL`, `APPROVED`, `CLOSED`, `VOIDED` | Formal bank reconciliation approval, close, and void lifecycle. |
| `BankReconciliationReviewAction` | `SUBMIT`, `APPROVE`, `REOPEN`, `CLOSE`, `VOID` | Append-only bank reconciliation review event action. |
| `WarehouseStatus` | `ACTIVE`, `ARCHIVED` | Warehouse availability for operational inventory movement entry. |
| `StockMovementType` | `OPENING_BALANCE`, `ADJUSTMENT_IN`, `ADJUSTMENT_OUT`, `TRANSFER_IN`, `TRANSFER_OUT`, `PURCHASE_RECEIPT_PLACEHOLDER`, `SALES_ISSUE_PLACEHOLDER` | Operational stock ledger movement classification; direct MVP creation allows opening balances only, while adjustments and transfers generate movement rows. |
| `InventoryAdjustmentStatus` | `DRAFT`, `APPROVED`, `VOIDED` | Inventory adjustment lifecycle. |
| `InventoryAdjustmentType` | `INCREASE`, `DECREASE` | Inventory adjustment quantity direction. |
| `WarehouseTransferStatus` | `POSTED`, `VOIDED` | Warehouse transfer lifecycle. |
| `PurchaseReceiptStatus` | `POSTED`, `VOIDED` | Operational purchase receipt lifecycle. |
| `SalesStockIssueStatus` | `POSTED`, `VOIDED` | Operational sales stock issue lifecycle. |
| `InventoryValuationMethod` | `MOVING_AVERAGE`, `FIFO_PLACEHOLDER` | Organization inventory valuation report policy. FIFO is saved as placeholder only. |
| `InventoryPurchasePostingMode` | `DISABLED`, `PREVIEW_ONLY` | Purchase receipt accounting mode marker. `PREVIEW_ONLY` keeps posting explicit/manual and never enables automatic receipt journals. |
| `PurchaseBillInventoryPostingMode` | `DIRECT_EXPENSE_OR_ASSET`, `INVENTORY_CLEARING` | Purchase bill compatibility mode for receipt clearing. Direct mode preserves current posting; clearing mode explicitly posts tracked bill lines to Inventory Clearing when finalized and is required for manual receipt asset posting. |
| `InventoryVarianceProposalStatus` | `DRAFT`, `PENDING_APPROVAL`, `APPROVED`, `POSTED`, `REVERSED`, `VOIDED` | Inventory variance proposal review, posting, reversal, and terminal lifecycle. |
| `InventoryVarianceProposalSourceType` | `CLEARING_VARIANCE`, `MANUAL` | Source classification for variance proposals. |
| `InventoryVarianceReason` | `PRICE_DIFFERENCE`, `QUANTITY_DIFFERENCE`, `RECEIPT_WITHOUT_CLEARING_BILL`, `CLEARING_BILL_WITHOUT_RECEIPT`, `REVERSED_RECEIPT_POSTING`, `MANUAL_ADJUSTMENT` | Reason classification for clearing/manual inventory variance proposals. |
| `InventoryVarianceProposalAction` | `CREATE`, `SUBMIT`, `APPROVE`, `POST`, `REVERSE`, `VOID` | Append-only proposal event action. |
| `DocumentType` | `SALES_INVOICE`, `CREDIT_NOTE`, `CUSTOMER_PAYMENT_RECEIPT`, `CUSTOMER_REFUND`, `CUSTOMER_STATEMENT`, `PURCHASE_ORDER`, `PURCHASE_BILL`, `PURCHASE_DEBIT_NOTE`, `SUPPLIER_PAYMENT_RECEIPT`, `SUPPLIER_REFUND`, `CASH_EXPENSE`, `REPORT_GENERAL_LEDGER`, `REPORT_TRIAL_BALANCE`, `REPORT_PROFIT_AND_LOSS`, `REPORT_BALANCE_SHEET`, `REPORT_VAT_SUMMARY`, `REPORT_AGED_RECEIVABLES`, `REPORT_AGED_PAYABLES`, `BANK_RECONCILIATION_REPORT` | Generated document classification. |
| `GeneratedDocumentStatus` | `GENERATED`, `FAILED`, `SUPERSEDED` | Archive status. |
| `AttachmentStorageProvider` | `DATABASE`, `LOCAL_PLACEHOLDER`, `S3_PLACEHOLDER` | Uploaded attachment storage provider marker; only database storage is active in the MVP, while storage readiness can report future S3-compatible configuration. |
| `AttachmentLinkedEntityType` | `SALES_INVOICE`, `CUSTOMER_PAYMENT`, `CREDIT_NOTE`, `CUSTOMER_REFUND`, `PURCHASE_BILL`, `SUPPLIER_PAYMENT`, `PURCHASE_DEBIT_NOTE`, `SUPPLIER_REFUND`, `PURCHASE_ORDER`, `CASH_EXPENSE`, `BANK_STATEMENT_IMPORT`, `BANK_STATEMENT_TRANSACTION`, `BANK_RECONCILIATION`, `PURCHASE_RECEIPT`, `SALES_STOCK_ISSUE`, `INVENTORY_ADJUSTMENT`, `WAREHOUSE_TRANSFER`, `INVENTORY_VARIANCE_PROPOSAL`, `CONTACT`, `ITEM`, `MANUAL_JOURNAL`, `OTHER` | Polymorphic source-record classification for uploaded attachments. |
| `AttachmentStatus` | `ACTIVE`, `DELETED` | Uploaded attachment lifecycle; delete is soft-delete only. |
| `ZatcaEnvironment` | `SANDBOX`, `SIMULATION`, `PRODUCTION` | ZATCA environment marker. |
| `ZatcaRegistrationStatus` | `NOT_CONFIGURED`, `DRAFT`, `READY_FOR_CSR`, `OTP_REQUIRED`, `CERTIFICATE_ISSUED`, `ACTIVE`, `SUSPENDED` | ZATCA profile/EGS state. |
| `ZatcaInvoiceType` | `STANDARD_TAX_INVOICE`, `SIMPLIFIED_TAX_INVOICE`, `CREDIT_NOTE`, `DEBIT_NOTE` | Future ZATCA invoice type. |
| `ZatcaInvoiceStatus` | `NOT_SUBMITTED`, `XML_GENERATED`, `READY_FOR_SUBMISSION`, `SUBMISSION_PENDING`, `CLEARED`, `REPORTED`, `REJECTED`, `FAILED` | Local ZATCA metadata lifecycle. |
| `ZatcaSubmissionType` | `COMPLIANCE_CHECK`, `CLEARANCE`, `REPORTING` | ZATCA submission/log type. |
| `ZatcaSubmissionStatus` | `PENDING`, `SUCCESS`, `REJECTED`, `FAILED` | ZATCA submission/log result. |
| `FiscalPeriodStatus` | `OPEN`, `CLOSED`, `LOCKED` | Fiscal period posting control state. |
| `NumberSequenceScope` | `JOURNAL_ENTRY`, `INVOICE`, `PURCHASE_ORDER`, `BILL`, `PAYMENT`, `CUSTOMER_REFUND`, `CREDIT_NOTE`, `DEBIT_NOTE`, `PURCHASE_DEBIT_NOTE`, `SUPPLIER_REFUND`, `CASH_EXPENSE`, `BANK_TRANSFER`, `BANK_RECONCILIATION`, `CONTACT`, `INVENTORY_ADJUSTMENT`, `WAREHOUSE_TRANSFER`, `PURCHASE_RECEIPT`, `SALES_STOCK_ISSUE`, `INVENTORY_VARIANCE_PROPOSAL` | Tenant numbering scopes. |

## Core Tenant And Security Models

| Model | Purpose | Important fields | Relationships | Accounting impact | Lifecycle/status | Known limitations |
| --- | --- | --- | --- | --- | --- | --- |
| `User` | Login identity. | `email`, `passwordHash`, `name`. | Memberships, created/posted records, COGS posted/reversed sales stock issues, receipt asset posted/reversed purchase receipts, inventory variance proposal actors, generated documents, uploaded/deleted attachments, auth tokens. | Tracks actors for accounting events. | No explicit status enum. | No MFA, refresh-token rotation, or advanced session management. |
| `Organization` | Tenant root. | `name`, `legalName`, `taxNumber`, `baseCurrency`, `timezone`. | Owns almost every business model, including inventory variance proposals, proposal events, attachments, auth tokens, and email outbox records. | Tenant boundary for ledgers/journals. | No explicit status enum. | No subscription/billing state. |
| `OrganizationMember` | User-to-org link. | `organizationId`, `userId`, `roleId`, `status`. | User, Organization, Role. | Controls org access and permission lookup. | `MembershipStatus`. | Invite acceptance is implemented with mock email only; no MFA or rate limiting. |
| `Role` | Stored permission set. | `name`, `permissions` JSON, `isSystem`. | Organization, members. | Runtime API/UI access control. | No lifecycle enum. | System roles are protected; no approval workflow. |
| `AuditLog` | Mutation audit trail. | `action`, `entityType`, `entityId`, `before`, `after`. | Organization, optional actor. | Supports auditability. | Append-only by convention. | Coverage should be verified per mutation. |
| `AuthToken` | Hashed email-token record for invitations and password reset. | `email`, `purpose`, `tokenHash`, `expiresAt`, `consumedAt`, optional organization/user/creator links. | Optional Organization, optional target User, optional creator User. | No accounting impact. | `AuthTokenPurpose`; consumed timestamp is terminal for a token. | No rate limiting or global session invalidation. |
| `EmailOutbox` | Mock/local email delivery record. | `toEmail`, `fromEmail`, `subject`, `templateType`, `bodyText`, optional `bodyHtml`, `status`, `provider`, provider metadata, `sentAt`. | Optional Organization. | No accounting impact. | `EmailDeliveryStatus`, `EmailTemplateType`. | Mock provider only; no SMTP/API provider, retries, bounces, DKIM/SPF, or domain auth. |

## Organization Setup Models

| Model | Purpose | Important fields | Relationships | Accounting impact | Lifecycle/status | Known limitations |
| --- | --- | --- | --- | --- | --- | --- |
| `Branch` | Sales/purchase branch. | `name`, `taxNumber`, address, `isDefault`. | Organization, invoices, credit notes, purchase orders, bills. | Branch context for documents and future taxes. | Active flag not present. | Multiple default branches can exist. |
| `Contact` | Customer/supplier master. | `type`, `name`, `displayName`, `taxNumber`, address, `isActive`. | Sales invoices, credit notes, purchase orders, bills, purchase receipts, sales stock issues, payments, refunds, optional inventory variance proposals. | AR/AP ledgers are contact-based; receipt/issue links are operational only. | Active boolean. | No bank details or duplicate management. |
| `OrganizationDocumentSettings` | PDF display settings. | titles, colors, visibility flags, template names. | One per organization. | Document presentation only. | No status enum. | Only standard renderer implemented. |
| `GeneratedDocument` | PDF archive. | `documentType`, `sourceType`, `sourceId`, `storageProvider`, `storageKey`, `contentBase64`, `contentHash`, `sizeBytes`. | Organization, generatedBy user. | Audit/archive of issued operational documents. | `GeneratedDocumentStatus`. | Base64 DB storage remains active; readiness/migration dry-run counts exist, but no object-storage migration executor exists. |
| `Attachment` | User-uploaded supporting file metadata and MVP content storage. | `linkedEntityType`, `linkedEntityId`, `filename`, `originalFilename`, `mimeType`, `sizeBytes`, `storageProvider`, `storageKey`, `contentBase64`, `contentHash`, `status`, notes, upload/delete actor timestamps. | Organization, uploadedBy User, deletedBy User; polymorphic source record is validated by service logic. | No journal impact; supports audit evidence on accounting and operational records. | `AttachmentStorageProvider`, `AttachmentLinkedEntityType`, `AttachmentStatus`. | Database/base64 storage remains active; S3-compatible adapter is readiness/stub groundwork only; no migration executor, virus scanning, OCR, retention policy, or email/ZATCA attachment submission. |
| `NumberSequence` | Tenant numbering. | `scope`, `prefix`, `nextNumber`, `padding`. | Organization. | Generates accounting document numbers. | No status enum. | No UI for sequence config. |

## Accounting Master Data

| Model | Purpose | Important fields | Relationships | Accounting impact | Lifecycle/status | Known limitations |
| --- | --- | --- | --- | --- | --- | --- |
| `Account` | Chart of accounts node. | `code`, `name`, `type`, `allowPosting`, `isSystem`, `isActive`, `parentId`. | Journal lines, items, invoice/credit/bill lines, payments/refunds, optional bank account profile, optional inventory accounting settings mappings including inventory clearing, inventory variance proposal debit/credit accounts. | All real postings reference accounts; manual COGS, explicit clearing-mode purchase bill finalization, explicit compatible purchase receipt asset posting, and explicit posted variance proposals use mapped/reviewed accounts. | Active/system flags. | Descendant cycle guard needs hardening; no `OTHER_INCOME` account type exists for adjustment gain mapping. |
| `BankAccountProfile` | Cash/bank metadata wrapper for a posting asset account. | `accountId`, `type`, `status`, `displayName`, masked account/IBAN, currency, opening balance metadata, `openingBalanceJournalEntryId`, `openingBalancePostedAt`. | Organization, one linked Account, opening-balance journal, incoming/outgoing transfers, statement imports/transactions, reconciliations. | Profile metadata itself does not post; optional opening-balance action posts one guarded journal and ledger balance is derived from posted journal lines. | `BankAccountStatus`. | No live feed, transfer fees, or FX transfer handling. |
| `BankTransfer` | Posted movement between two active bank/cash profiles. | `transferNumber`, from/to profile and account ids, `transferDate`, `amount`, `journalEntryId`, `voidReversalJournalEntryId`. | Organization, from/to BankAccountProfile, from/to Account, posting/reversal JournalEntry, creator. | Posts Dr destination bank/cash and Cr source bank/cash; void posts the reverse once. | `BankTransferStatus`. | No transfer fee, FX gain/loss, or scheduled transfers. |
| `BankStatementImport` | Local bank statement import batch. | `bankAccountProfileId`, `filename`, `sourceType`, `status`, statement dates, opening/closing statement balances, `rowCount`, `importedAt`. | Organization, BankAccountProfile, importedBy User, statement transactions. | Import itself creates no journals; preview validates CSV/JSON rows before writing and real import rejects closed reconciliation periods. | `BankStatementImportStatus`. | Pasted CSV/JSON only; attachment entity validation exists, but no statement-file parser, OFX/CAMT/MT940 parser, or imported statement file storage workflow exists yet. |
| `BankStatementTransaction` | Imported statement row for manual reconciliation. | `transactionDate`, `description`, `reference`, `type`, `amount`, `status`, matched/categorized journal links, `rawData`. | Organization, import batch, BankAccountProfile, optional matched JournalLine/JournalEntry, optional categorized Account, optional created JournalEntry, reconciliation items. | Manual match creates no journal; categorization creates a posted balanced journal; closed reconciliation periods block status-changing actions. | `BankStatementTransactionStatus`, `BankStatementTransactionType`, optional `BankStatementMatchType`. | No automatic/ML matching, bank feeds, or split categorization. |
| `BankReconciliation` | Formal bank account reconciliation record. | `reconciliationNumber`, `bankAccountProfileId`, `periodStart`, `periodEnd`, statement opening/closing balances, `ledgerClosingBalance`, `difference`, submit/approve/reopen/close/void actors and timestamps, notes. | Organization, BankAccountProfile, created/submitted/approved/reopened/closed/voided users, reconciliation items, review events. | Creates no journals; submit requires zero difference/no unmatched rows, approval precedes close, close snapshots statement rows and locks the period; report PDFs are archived as generated documents. | `BankReconciliationStatus`. | Basic linked attachments exist; no full approval queue or statement parser tied to attachments. |
| `BankReconciliationReviewEvent` | Append-only reconciliation reviewer history. | `reconciliationId`, `actorUserId`, `action`, `fromStatus`, `toStatus`, `notes`, `createdAt`. | Organization, BankReconciliation, optional actor User. | Records submit/approve/reopen/close/void transitions without posting journals. | `BankReconciliationReviewAction`. | Does not enforce full dual-control policy beyond current same-submitter approval guard. |
| `BankReconciliationItem` | Immutable statement transaction snapshot captured at reconciliation close. | `reconciliationId`, `statementTransactionId`, `statusAtClose`, `amount`, `type`. | Organization, BankReconciliation, BankStatementTransaction. | No posting impact; supports review history and closed-period lock checks. | Inherits parent reconciliation lifecycle. | Snapshot is row-level only; no attached bank statement document. |
| `Warehouse` | Operational inventory location. | `code`, `name`, `status`, address fields, `countryCode`, `isDefault`. | Organization, StockMovement, InventoryAdjustment, WarehouseTransfer as source/destination, PurchaseReceipt, SalesStockIssue. | No direct accounting impact; active warehouses can receive opening balances, approved adjustments, transfers, purchase receipts, and sales issues. | `WarehouseStatus`. | No bin/location hierarchy or warehouse accounting. |
| `StockMovement` | Operational stock ledger row. | `itemId`, `warehouseId`, `movementDate`, `type`, positive `quantity`, optional `unitCost`, optional `totalCost`, reference fields, creator. | Organization, Item, Warehouse, optional createdBy User, optional adjustment/transfer/receipt/issue source links. | No journal entry; quantity ledger only. | Direction is derived from `StockMovementType`. | No COGS, valuation accounting, automatic receipts/issues, landed cost, or serial/batch tracking. |
| `InventoryAdjustment` | Controlled inventory quantity adjustment. | `adjustmentNumber`, `itemId`, `warehouseId`, `type`, `status`, `adjustmentDate`, positive `quantity`, optional costs, `reason`, approval/void actor timestamps, stock movement links. | Organization, Item, Warehouse, optional created/approved/voided Users, generated StockMovement rows. | No journal entry; approval and void create operational stock movements only. | `InventoryAdjustmentStatus`, `InventoryAdjustmentType`. | Basic linked attachments exist; no reason-code catalog, valuation accounting, or dual-control approval queue. |
| `WarehouseTransfer` | Posted stock movement between two warehouses. | `transferNumber`, `itemId`, `fromWarehouseId`, `toWarehouseId`, `status`, `transferDate`, positive `quantity`, optional costs, `description`, stock movement links. | Organization, Item, source/destination Warehouse, optional creator User, generated StockMovement rows. | No journal entry; create and void write paired operational stock movements only. | `WarehouseTransferStatus`. | No in-transit status, shipping documents, bin/location support, valuation accounting, or serial/batch tracking. |
| `PurchaseReceipt` | Posted operational stock receipt with optional manual asset posting links. | `receiptNumber`, optional `purchaseOrderId`/`purchaseBillId`, `supplierId`, `warehouseId`, `receiptDate`, `status`, notes, actor timestamps, optional `inventoryAssetJournalEntryId`, `inventoryAssetReversalJournalEntryId`, asset posted/reversed timestamps and users. | Organization, supplier Contact, optional PurchaseOrder/PurchaseBill, Warehouse, creator User, receipt lines, optional asset JournalEntry, optional asset reversal JournalEntry, optional inventory variance proposals. | Receipt creation creates stock movements only; explicit compatible asset posting creates linked Dr Inventory Asset / Cr Inventory Clearing journal; clearing reports read these links and variance rows can feed reviewed proposals. | `PurchaseReceiptStatus`; asset posting is controlled by linked journal fields. | No automatic receipt posting, direct-mode receipt posting, automatic variance journals, landed cost, supplier delivery document, or serial/batch tracking. |
| `PurchaseReceiptLine` | Purchase receipt line. | `receiptId`, `itemId`, optional source PO/bill line ids, positive `quantity`, optional `unitCost`, stock movement links. | Organization, PurchaseReceipt, Item, optional PurchaseOrderLine/PurchaseBillLine, generated StockMovement rows. | No journal entry from the line itself; parent receipt can manually post reviewed inventory asset when compatible. | Inherits parent receipt lifecycle. | No lot/serial tracking, landed-cost allocation, or accounting-grade cost layer. |
| `SalesStockIssue` | Posted operational sales issue with optional manual COGS posting links. | `issueNumber`, `salesInvoiceId`, `customerId`, `warehouseId`, `issueDate`, `status`, notes, actor timestamps, optional `cogsJournalEntryId`, `cogsReversalJournalEntryId`, COGS posted/reversed timestamps and users. | Organization, SalesInvoice, customer Contact, Warehouse, creator User, issue lines, optional COGS JournalEntry, optional COGS reversal JournalEntry. | Operational issue creates stock movements only; explicit manual COGS posting creates linked Dr COGS / Cr Inventory Asset journal. | `SalesStockIssueStatus`; COGS posting is controlled by linked journal fields. | No automatic COGS posting, delivery document, returns workflow, FIFO, landed cost, or serial/batch tracking. |
| `SalesStockIssueLine` | Sales stock issue line. | `issueId`, `itemId`, optional `salesInvoiceLineId`, positive `quantity`, optional `unitCost`, stock movement links. | Organization, SalesStockIssue, Item, optional SalesInvoiceLine, generated StockMovement rows. | No journal entry from the line itself; parent issue can manually post reviewed COGS. | Inherits parent issue lifecycle. | No delivery fulfillment states, automatic COGS posting, or lot/serial tracking. |
| `InventorySettings` | Organization inventory reporting and accounting posting readiness policy. | `valuationMethod`, `allowNegativeStock`, `trackInventoryValue`, `enableInventoryAccounting`, optional inventory asset/COGS/inventory clearing/adjustment gain/adjustment loss account ids, `purchaseReceiptPostingMode`. | Organization one-to-one; optional Account mappings for inventory asset, COGS, inventory clearing, adjustment gain, and adjustment loss. | Controls operational report behavior, purchase receipt preview/readiness audit, purchase bill clearing-mode validation, manual COGS readiness, manual compatible receipt asset posting, and clearing report account mapping; does not auto-post inventory journals. | `InventoryValuationMethod`, `InventoryPurchasePostingMode`. | FIFO is placeholder only; no automatic posting, direct-mode receipt posting, GL inventory valuation report, or automatic variance posting. |
| `InventoryVarianceProposal` | Accountant-reviewed inventory clearing/manual variance proposal. | `proposalNumber`, `sourceType`, `reason`, `status`, optional purchase bill/receipt/supplier links, `proposalDate`, positive `amount`, explicit `debitAccountId`/`creditAccountId`, actor timestamps, `journalEntryId`, `reversalJournalEntryId`, notes/reasons. | Organization, optional PurchaseBill/PurchaseReceipt/Contact, debit/credit Account, actor Users, optional posted JournalEntry/reversal, events. | No journal until explicit post from `APPROVED`; posting creates Dr debit account / Cr credit account; reversal creates one reversal journal. | `InventoryVarianceProposalStatus`, `InventoryVarianceProposalSourceType`, `InventoryVarianceReason`. | No automatic variance posting, no landed cost policy, and no duplicate proposal prevention beyond accountant review. |
| `InventoryVarianceProposalEvent` | Append-only variance proposal lifecycle event. | `proposalId`, `actorUserId`, `action`, `fromStatus`, `toStatus`, `notes`, `createdAt`. | Organization, InventoryVarianceProposal, optional actor User. | No posting; audit history for create/submit/approve/post/reverse/void. | `InventoryVarianceProposalAction`. | Does not enforce dual-control beyond role permissions. |
| `TaxRate` | VAT/tax rate. | `scope`, `category`, `rate`, `isActive`, `isSystem`. | Invoice, credit note, purchase bill lines, items, journal lines. | Calculates tax and links tax lines. | Active/system flags. | VAT reporting not implemented. |
| `Item` | Product/service catalog. | `type`, `status`, `sellingPrice`, `purchaseCost`, account/tax defaults, `inventoryTracking`, optional `reorderPoint`, optional `reorderQuantity`. | Invoice, credit note, purchase order, purchase bill, purchase receipt, sales stock issue lines, stock movements, inventory adjustments, warehouse transfers. | Defaults accounts/taxes; inventory movements and reorder fields are operational only and do not post stock accounting yet. | `ItemStatus`. | No automatic sales/purchase stock movements, COGS, or valuation accounting. |
| `FiscalPeriod` | Accounting posting period. | `name`, `startsOn`, `endsOn`, `status`. | Organization. | Controls posting windows through the fiscal period guard. | `FiscalPeriodStatus`. | No unlock/admin approval, fiscal year wizard, or retained earnings close. |

## Journal Models

| Model | Purpose | Important fields | Relationships | Accounting impact | Lifecycle/status | Known limitations |
| --- | --- | --- | --- | --- | --- | --- |
| `JournalEntry` | Header for accounting postings. | `entryNumber`, `status`, `entryDate`, `totalDebit`, `totalCredit`, `reversalOfId`. | Lines, source workflow records, reversal entry, optional sales stock issue COGS links, optional purchase receipt asset links, optional inventory variance proposal links. | Source of GL truth, including manually posted stock issue COGS, manually posted receipt inventory asset journals, and explicitly posted inventory variance proposal journals. | `JournalEntryStatus`. | No period lock enforcement outside guarded posting services. |
| `JournalLine` | Debit/credit line. | `accountId`, `debit`, `credit`, `currency`, `exchangeRate`, optional `taxRateId`. | Journal entry, account, tax rate. | GL line-level impact. | Inherits entry lifecycle. | No dimensions beyond account/tax. |

## Sales And AR Models

| Model | Purpose | Important fields | Relationships | Accounting impact | Lifecycle/status | Known limitations |
| --- | --- | --- | --- | --- | --- | --- |
| `SalesInvoice` | Customer invoice. | `invoiceNumber`, `customerId`, dates, totals, `balanceDue`, `journalEntryId`, `reversalJournalEntryId`. | Lines, payments, credit notes, stock issues, ZATCA metadata. | Finalization posts Dr AR, Cr revenue/VAT; stock issue is separate operational movement only. | `SalesInvoiceStatus`. | No recurring invoices; no automatic stock issue. |
| `SalesInvoiceLine` | Invoice line. | `quantity`, `unitPrice`, `discountRate`, `taxRateId`, calculated amounts. | Invoice, item, account, tax rate, optional sales stock issue lines. | Revenue and tax basis. | Inherits invoice lifecycle. | Stock issue is manual and does not post COGS. |
| `CustomerPayment` | Customer receipt. | `paymentNumber`, `amountReceived`, `unappliedAmount`, `accountId`, journal/reversal links. | Allocations, unapplied allocations, refunds. | Posts Dr bank/cash, Cr AR. | `CustomerPaymentStatus`. | No gateway/bank reconciliation. |
| `CustomerPaymentAllocation` | Payment-to-invoice match at creation. | `paymentId`, `invoiceId`, `amountApplied`. | Payment, invoice. | Reduces invoice `balanceDue`; payment already posted. | Immutable row. | No reversal except payment void. |
| `CustomerPaymentUnappliedAllocation` | Later overpayment application. | `paymentId`, `invoiceId`, `amountApplied`, `reversedAt`, `reversalReason`. | Payment, invoice, reversedBy. | Matching only; no journal. | Active if `reversedAt` null. | No automatic matching. |
| `CreditNote` | Customer credit note. | `creditNoteNumber`, `customerId`, optional `originalInvoiceId`, totals, `unappliedAmount`, journal/reversal links. | Lines, allocations, refunds. | Finalization posts Dr revenue/VAT, Cr AR. | `CreditNoteStatus`. | ZATCA credit note XML not implemented. |
| `CreditNoteLine` | Credit note line. | Quantity/price/discount/tax/calculated amounts. | Credit note, item, account, tax rate. | Revenue/tax reversal basis. | Inherits credit note lifecycle. | No inventory return. |
| `CreditNoteAllocation` | Credit note-to-invoice match. | `creditNoteId`, `invoiceId`, `amountApplied`, reversal metadata. | Credit note, invoice, reversedBy. | Matching only; no journal. | Active if `reversedAt` null. | No automatic suggestion. |
| `CustomerRefund` | Manual customer refund. | `refundNumber`, `sourceType`, source payment/credit note, `amountRefunded`, account, journal/reversal links. | Contact, source payment or credit note, account. | Posts Dr AR, Cr bank/cash. | `CustomerRefundStatus`. | No payment gateway or bank reconciliation. |

## Purchases And AP Models

| Model | Purpose | Important fields | Relationships | Accounting impact | Lifecycle/status | Known limitations |
| --- | --- | --- | --- | --- | --- | --- |
| `PurchaseOrder` | Supplier purchase order. | `purchaseOrderNumber`, `supplierId`, dates, totals, status timestamps, `convertedBillId`. | Lines, supplier, branch, converted bill, purchase receipts. | No journal entries; source document for later bill and operational stock receipt. | `PurchaseOrderStatus`. | No partial billing, approval chain, automatic stock receipt, or supplier delivery document. |
| `PurchaseOrderLine` | Purchase order line. | Optional item/account, quantity/price/discount/tax/calculated amounts. | Purchase order, item, optional account, tax rate, optional receipt lines. | No posting; account pre-fills converted bill line. | Inherits PO lifecycle. | Account can be missing until conversion, but conversion then requires one. |
| `PurchaseBill` | Supplier bill. | `billNumber`, `supplierId`, optional `purchaseOrderId`, dates, totals, `balanceDue`, `inventoryPostingMode`, journal/reversal links. | Lines, source purchase order, supplier payment allocations, purchase receipts, optional inventory variance proposals. | Direct-mode finalization posts Dr expense/asset/VAT receivable, Cr AP; inventory-clearing mode posts Dr Inventory Clearing for tracked lines, Dr selected accounts for non-inventory lines, Dr VAT, Cr AP. Compatible linked receipts can be manually posted separately to Dr Inventory Asset / Cr Inventory Clearing; clearing differences can feed reviewed variance proposals. | `PurchaseBillStatus`, `PurchaseBillInventoryPostingMode`. | No multi-PO clearing workflow, automatic receipt GL posting, direct-mode receipt posting, or historical direct-bill migration. |
| `PurchaseBillLine` | Bill line. | Quantity/price/discount/tax/calculated amounts. | Bill, item, account, tax rate, optional receipt lines. | Expense/asset and VAT basis. | Inherits bill lifecycle. | Receipt is manual and does not debit inventory asset or allocate landed cost. |
| `SupplierPayment` | Supplier payment. | `paymentNumber`, `supplierId`, `amountPaid`, `unappliedAmount`, account, journal/reversal links. | Supplier, paid-through account, allocations. | Posts Dr AP, Cr bank/cash. | `SupplierPaymentStatus`. | No AP overpayment application/refund workflow beyond MVP unapplied storage. |
| `SupplierPaymentAllocation` | Supplier payment-to-bill match. | `paymentId`, `billId`, `amountApplied`. | Supplier payment, purchase bill. | Reduces bill `balanceDue`; payment already posted. | Immutable row. | No separate allocation reversal; payment void restores. |

## ZATCA Models

| Model | Purpose | Important fields | Relationships | Accounting impact | Lifecycle/status | Known limitations |
| --- | --- | --- | --- | --- | --- | --- |
| `ZatcaOrganizationProfile` | Seller profile. | environment, registration status, seller/VAT/address fields. | Organization, EGS units. | Supports invoice compliance metadata. | `ZatcaRegistrationStatus`. | Needs official validation. |
| `ZatcaEgsUnit` | EGS/device record. | device serial, CSR/private key/CSIDs, active flag, last ICV/hash. | Profile, invoice metadata, submission logs. | Tracks local hash-chain state. | `ZatcaRegistrationStatus`. | Private key DB field is dev-only; real keys need KMS. |
| `ZatcaInvoiceMetadata` | Per-invoice ZATCA local metadata. | invoice UUID, status, ICV, previous hash, invoice hash, XML/QR base64. | Sales invoice, EGS, logs. | Does not change GL; compliance metadata only. | `ZatcaInvoiceStatus`. | XML/hash/signing are not official production implementation. |
| `ZatcaSubmissionLog` | Submission/onboarding attempt log. | submission type/status, request/response payloads, response/error codes. | Organization, invoice metadata, EGS. | Audit trail only. | `ZatcaSubmissionStatus`. | Payload logging must continue redacting secrets. |
