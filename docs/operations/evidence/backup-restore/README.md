# ARC-02 local PostgreSQL recovery evidence

The evidence in this directory was generated by the guarded local recovery drill. Each run creates two artifacts:

- JSON is the machine-readable result.
- Markdown is the human-readable result.

The latest source-aligned run is `local-postgres-drill-2026-07-19T19-24-18-323Z.{json,md}`. It ran from commit `9bff09af`, restored a fresh custom-format backup into a distinct local disposable database, and passed every recorded check.

Its backup artifact measured 1,178,293 bytes and had SHA-256 `4c2948ee6bf9fe0d981fc261178a48e43ff654f053d62215034f4558c5331fc0`. The backup itself is intentionally not committed.

All records use synthetic data. Database targets are redacted, classified as local/disposable, and hosted, production, beta, and staging mutation is blocked/not attempted. These artifacts do not establish hosted recovery, object-storage recovery, RPO/RTO, or production readiness.
