"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { apiRequest } from "@/lib/api";
import { FixedAssetNav } from "@/components/fixed-assets/fixed-assets-page";
import { LedgerAlert, LedgerButton, LedgerFieldLabel, LedgerInput, LedgerPage, LedgerPageBody, LedgerPageHeader } from "@/components/ui/ledger-system";
export default function NewDepreciationRunPage() { const router = useRouter(); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); const data = new FormData(event.currentTarget); try { const run = await apiRequest<{ id: string }>("/fixed-assets/depreciation-runs/preview", { method: "POST", body: { depreciationDate: String(data.get("depreciationDate")), idempotencyKey: String(data.get("idempotencyKey")) } }); router.push(`/fixed-assets/depreciation-runs/${run.id}`); } catch (e) { setError(e instanceof Error ? e.message : "Unable to preview depreciation run."); } finally { setBusy(false); } } return <LedgerPage><LedgerPageHeader eyebrow="Fixed assets" title="Preview depreciation run" description="Preview is non-posting. Review the generated lines before the accountant posts the run." /><LedgerPageBody><FixedAssetNav />{error ? <LedgerAlert tone="danger" title="Preview failed">{error}</LedgerAlert> : null}<form onSubmit={submit} className="max-w-xl space-y-4 rounded-md border border-line bg-panel p-5 shadow-panel"><div><LedgerFieldLabel htmlFor="depreciationDate">Depreciation date</LedgerFieldLabel><LedgerInput id="depreciationDate" name="depreciationDate" type="date" required /></div><div><LedgerFieldLabel htmlFor="idempotencyKey">Review key</LedgerFieldLabel><LedgerInput id="idempotencyKey" name="idempotencyKey" required placeholder="2026-07-fixed-assets" /><p className="mt-1 text-xs text-steel">Reuse the same key to safely reopen the same preview.</p></div><div className="flex justify-end gap-2"><LedgerButton href="/fixed-assets/depreciation-runs" variant="secondary">Cancel</LedgerButton><LedgerButton type="submit" variant="primary" disabled={busy}>{busy ? "Building preview…" : "Build preview"}</LedgerButton></div></form></LedgerPageBody></LedgerPage>; }
