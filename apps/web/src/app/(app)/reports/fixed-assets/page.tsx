"use client";
import { useEffect, useState } from "react";
import { FixedAssetNav } from "@/components/fixed-assets/fixed-assets-page";
import { apiRequest } from "@/lib/api";
import { useActiveOrganizationId } from "@/hooks/use-active-organization";
import { LedgerAlert, LedgerDataTable, LedgerLoadingState, LedgerPage, LedgerPageBody, LedgerPageHeader, LedgerSection, LedgerStatusBadge } from "@/components/ui/ledger-system";
type Register = Array<{ assetNumber: string; name: string; status: string; originalCost: string; accumulatedDepreciation: string; carryingAmount: string }>;
type Reconciliation = { register: { cost: string; accumulatedDepreciation: string; carryingAmount: string }; generalLedger: { fixedAssetCost: string; accumulatedDepreciation: string; depreciationExpense: string }; differences: { cost: string; accumulatedDepreciation: string; depreciationExpense: string }; reconciled: boolean };
export default function FixedAssetReportsPage() { const organizationId = useActiveOrganizationId(); const [register, setRegister] = useState<Register | null>(null); const [reconciliation, setReconciliation] = useState<Reconciliation | null>(null); const [error, setError] = useState(""); useEffect(() => { if (organizationId) Promise.all([apiRequest<Register>("/reports/fixed-assets/register"), apiRequest<Reconciliation>("/reports/fixed-assets/reconciliation")]).then(([r, recon]) => { setRegister(r); setReconciliation(recon); }).catch((e: unknown) => setError(e instanceof Error ? e.message : "Unable to load fixed-asset reports.")); }, [organizationId]); return <LedgerPage><LedgerPageHeader eyebrow="Reports / Fixed assets" title="Fixed asset reports" description="Register, depreciation, disposal, and GL reconciliation evidence for accountant review." /><LedgerPageBody><FixedAssetNav />{error ? <LedgerAlert tone="danger" title="Reports unavailable">{error}</LedgerAlert> : null}{!register || !reconciliation ? <LedgerLoadingState title="Loading reports" /> : <><LedgerSection title="Register report" description={`${register.length} assets in the current register.`}><LedgerDataTable minWidth="820px"><thead className="bg-mist text-left text-xs uppercase tracking-wide text-steel"><tr>{["Asset", "Status", "Cost", "Accumulated depreciation", "Carrying value"].map((heading) => <th key={heading} className="px-4 py-3">{heading}</th>)}</tr></thead><tbody className="divide-y divide-line">{register.map((row) => <tr key={row.assetNumber}><td className="px-4 py-3 font-semibold">{row.assetNumber}<div className="text-xs font-normal text-steel">{row.name}</div></td><td className="px-4 py-3"><LedgerStatusBadge>{row.status}</LedgerStatusBadge></td><td className="px-4 py-3 text-right font-mono text-xs">{row.originalCost}</td><td className="px-4 py-3 text-right font-mono text-xs">{row.accumulatedDepreciation}</td><td className="px-4 py-3 text-right font-mono text-xs font-semibold">{row.carryingAmount}</td></tr>)}</tbody></LedgerDataTable></LedgerSection><LedgerSection title="GL reconciliation" description="Register balances are compared with mapped fixed-asset accounts; differences stay visible until resolved."><div className="grid gap-3 sm:grid-cols-3">{[["Register cost", reconciliation.register.cost, reconciliation.differences.cost], ["Accumulated depreciation", reconciliation.register.accumulatedDepreciation, reconciliation.differences.accumulatedDepreciation], ["Depreciation expense", reconciliation.generalLedger.depreciationExpense, reconciliation.differences.depreciationExpense]].map(([label, value, difference]) => <div key={label} className="rounded-md border border-line bg-mist p-3"><div className="text-xs font-semibold uppercase tracking-wide text-steel">{label}</div><div className="mt-1 font-mono text-sm">{value}</div><div className="mt-1 text-xs text-steel">Difference: {difference}</div></div>)}</div><div className="mt-4"><LedgerStatusBadge tone={reconciliation.reconciled ? "success" : "danger"}>{reconciliation.reconciled ? "Reconciled" : "Difference requires review"}</LedgerStatusBadge></div></LedgerSection></>}</LedgerPageBody></LedgerPage>; }
